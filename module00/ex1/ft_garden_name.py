def ft_garden_name():
    print("Enter garden name:")
    name = input()
    print(f"Garden: {name}")
    print("Status: Growing well!")

    # line 4 :
    # A formatted string literal or f-string is a string literal 
    # that is prefixed with f or F.
    # These strings may contain replacement fields, which are
    #  expressions delimited by curly braces {}.
    # While other string literals always have a constant value,
    #  formatted strings are really expressions
    # evaluated at run time.
